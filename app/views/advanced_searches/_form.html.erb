<h1>Lista de Pousadas</h1>

<h2>Busca Avançada</h2>

<div id="advanced_search">
  <%= form_with(url: advanced_search_inns_results_path, method: :get) do |f| %>
    <div>
      <%= f.label :pets, 'Aceita pets' %>
      <%= f.check_box :pets, { checked: @params[:pets] } %>
    </div>
    
    <div>
      <%= f.label :bathroom, 'Banheiro' %>
      <%= f.check_box :bathroom, { checked: @params[:bathroom] } %>
    </div>

    <div>
      <%= f.label :balcony, 'Varanda' %>
      <%= f.check_box :balcony, { checked: @params[:balcony] } %>
    </div>

    <div>
      <%= f.label :air, 'Ar condicionado' %>
      <%= f.check_box :air, { checked: @params[:air] } %>
    </div>

    <div>
      <%= f.label :tv, 'TV' %>
      <%= f.check_box :tv, { checked: @params[:tv] } %>
    </div>

    <div>
      <%= f.label :wardrobe, 'Guarda-roupa' %>
      <%= f.check_box :wardrobe, { checked: @params[:wardrobe] } %>
    </div>

    <div>
      <%= f.label :safe, 'Cofre' %>
      <%= f.check_box :safe, { checked: @params[:safe] } %>
    </div>

    <div>
      <%= f.label :accessible, 'Acessibilidade' %>
      <%= f.check_box :accessible, { checked: @params[:accessible] } %>
    </div><br >
    
    <div>
      <%= f.submit 'Buscar' %>
    </div>
  <% end %>
</div>