<h1>Adicionar Avaliação</h1>

<p>Deixe sua avaliação sobre o quarto</p>
<p>Quanto mais detalhada, melhor!</p>
<p>De uma nota de 0 a 5, onde 0 é péssima e 5 é ótima</p>

<%= form_with(model: @review, url: inn_room_reservation_reviews_path) do |f| %>
  <div>
    <%= f.label :rating %>
    <%= f.number_field :rating %>
  </div>

  <div>
    <%= f.label :comment %>
    <%= f.text_area :comment %>
  </div>

  <div>
    <%= f.submit 'Enviar Avaliação' %>
  </div>
<% end %>